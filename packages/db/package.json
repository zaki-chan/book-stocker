{
    "name": "db",
    "version": "1.0.0",
    "private": true,
    "dependencies": {
        "@prisma/client": "^6.18.0",
        "prisma": "^6.18.0"
    },
    "scripts": {
        "migrate-script": "npx dotenv -f ./.env -- pnpm exec prisma migrate dev",
        "generate-script": "npx dotenv -f ./.env -- pnpm exec prisma generate --schema ./prisma/schema.prisma",
        "postinstall": "dotenv -c -f ./.env -- prisma generate --schema ./prisma/schema.prisma"
    }
}