{
  "name": "book-stocker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "npm-run-all -l --parallel dev:client dev:server",
    "dev:client": "pnpm --filter client dev",
    "dev:server": "pnpm --filter server dev",
    "db:migrate": "pnpm --filter db run migrate-script",
    "db:generate": "pnpm --filter db run generate-script",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.19.0",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "pnpm": {
    "patchedDependencies": {}
  },
  "dependencies": {
    "@types/node": "^24.9.1",
    "dotenv-cli": "^11.0.0",
    "prisma": "^6.18.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
